export declare class MiniMarkdown {
    makeHtml(markdown: string): string;
    private htmlItalics;
    private htmlParticularOriginalLanguage;
    private htmlOriginalLanguage;
    makeTex(markdown: string): string;
    private texItalics;
    private texParticularOriginalLanguage;
    private texOriginalLanguage;
}
